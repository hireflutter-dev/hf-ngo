 import 'package:Gifts/src/common/providers/ngos_provider.dart';
import 'package:Gifts/src/screens/event_detail_screen.dart';
import 'package:flutter/material.dart';
import 'package:provider/provider.dart';

class NgoListForEvent extends StatelessWidget {
  static const routeName = '/ngoListForEvent';
  // final String title;
  

  // NgoListForEvent(
  //   this.ngoTitle,
  //   this.bankAccountNumber,
  //   this.ifsc,
  // );

  @override
  Widget build(BuildContext context) {
    return ListView.builder(
          itemBuilder: (context, i) {
            

            return ListTile(
              title: Text(_ngo.ngoTitle),
              subtitle: Text('cause: ${_ngo.causeTitle}'),
              onTap: () {
                
                
              },
            );
          },
        );
  }
}
